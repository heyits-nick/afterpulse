[project]
name = "afterpulse-agent"
version = "0.1.0"
description = "Guava post-event wellness check-in for the AfterPulse demo"
requires-python = ">=3.12"
dependencies = ["guava-sdk>=0.40,<0.41"]

[tool.uv]
package = false
